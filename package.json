{
  "name": "dev-connect",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "bootstrap": "npm install --workspaces",
    "dev": "echo \"Run workspace-specific dev scripts (e.g. cd apps/frontend && npm run dev)\" && exit 1",
    "lint": "echo \"add lint scripts later\" && exit 1"
  }
}
